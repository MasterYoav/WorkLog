// Temporarily disable the starter Tabs group to avoid it being shown by default.
// We will re-enable or move it later once our auth/clock flow is in place.
export default function TabLayout() {
  return null;
}
